#include "main.h"
/**
 * add - adds two integers together
 * @a: first int to add to the sum
 * @b: second int to add to the sum
 * Return: returns the result of the addition
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
